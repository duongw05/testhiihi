export type STAFF_SUMMARY = {
    totalEmp: number,
    totalBirthOnMonth: number,
    totalOnWorking: number,
    totalOnRetired: number
}

export type STAFF_ATTENDANCE_SUMMARY = {
    onTime: number,
    notCheckInYet: number,
    lateCheckIn: number,
}